import TreeNode = require('bintree/treenode/TreeNode');

declare class NodeTree {
	private root: TreeNode;
	private size: number;
	private depth: number;

	constructor();

	public getRoot(): TreeNode;
	public getSize(): number;
	public getDepth(): number;

	public setRoot(root: TreeNode);
	public setSize(size: number);
	public setDepth(depth: number);

	private insertionHelper(root: TreeNode, node: TreeNode, depth: number): TreeNode;
	private displayPreorderHelper(node: TreeNode);
	private displayInorderHelper(node: TreeNode);
	private displayPostorderHelper(node: TreeNode);

	public insert(node: TreeNode): TreeNode;
	public displayPreorder();
	public displayInorder();
	public displayPostorder();

}
export = NodeTree;